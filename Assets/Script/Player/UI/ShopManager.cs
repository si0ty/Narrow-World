using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/*
public class InventoryManager : MonoBehaviour {
    public int goldAmount;
    public Text goldText;
  
    // Use this for initialization


    void Start() {

       UpdateAmount();
    }

    public void BuyUnit(int unitPrice) {
        // Kontostand überprüfen
        if (goldAmount >= unitPrice) {
            Debug.Log("Unit wurde gekauft");
            // Item zum Inventar hinzufügen
            myInventory.GetUnit();
            // Kontostand neu berechnen
            goldAmount = goldAmount - unitPrice;
            Debug.Log("Gold: " + goldAmount);
            // Kontostand aktualisieren (Textfeld)
            UpdateAmount();
        }
    }
    public void BuyUpgrade(int unitPrice) {
        // Kontostand überprüfen
        if (goldAmount >= unitPrice) {
            Debug.Log("Upgrade wurde gekauft");
            // Char im Inventory aktivieren
            myInventory.GetUpgrade();
            // Kontostand neu berechnen
            goldAmount = goldAmount - unitPrice;
            Debug.Log("Gold: " + goldAmount);
            // Kontostand aktualisieren (Textfeld)
            UpdateAmount();
            // Button deaktivieren
        }
    }
    void UpdateAmount() {
        goldText.text = goldAmount.ToString();
    }
}



    */







